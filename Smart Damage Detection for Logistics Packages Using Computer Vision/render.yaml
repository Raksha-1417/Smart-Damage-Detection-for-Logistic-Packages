databases:
  - name: logivision-db
    databaseName: logivision
    user: logivision_admin
    plan: free

services:
  # ── Backend ───────────────────────────────────────────────────────────────
  - type: web
    name: logivision-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: POSTGRES_DSN
        fromDatabase:
          name: logivision-db
          property: connectionString
      # Set these three in the Render dashboard (Mark as secret):
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: S3_BUCKET_NAME
        value: damage-detection-images-s3
      - key: AWS_REGION
        value: eu-north-1
      - key: SECRET_KEY
        generateValue: true
      - key: ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 30
      - key: CONF_THRESH
        value: 0.25
      - key: IMG_SZ
        value: 640

  # ── Frontend ──────────────────────────────────────────────────────────────
  - type: web
    name: logivision-frontend
    env: static
    # build.sh injects API_URL into environment.prod.ts then runs npm build
    buildCommand: cd frontend/angular-app && bash build.sh
    # Angular 17+ @angular/build:application outputs to dist/<project>/browser
    staticPublishPath: frontend/angular-app/dist/damage-detection-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      # Set to your backend URL after first deploy, e.g.:
      #   https://logivision-backend.onrender.com/api
      - key: API_URL
        sync: false
